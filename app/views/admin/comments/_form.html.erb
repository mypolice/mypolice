<%= form_for([:admin,@comment]) do |f| %>
<%= render "shared/error_message", :target => @comment %>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>

