<%= form_for([:admin,@response]) do |f| %>
  <%= render "shared/error_message", :target => [@response] %>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
