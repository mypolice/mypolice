-if target.class <= Array
  -a_tartget = target
-else 
  -a_target =[target]
-target.each do |t|
  -if t.errors.any?
    #errorExplanation
      %h2
        = pluralize(t.errors.count, "error")
        prohibited this record from being saved:
      %ul
        -t.errors.full_messages.each do |msg| 
          %li 
            = msg

